<h1>This is a test</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <input type="checkbox" name="<%= task.title.downcase %>" />
      <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>
